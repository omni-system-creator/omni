npm run build:app
npx cap sync

打开android studio：
npx cap open android

生成图标和启动页：
- 图标 ：准备一张 1024x1024 像素的 PNG 图片，重命名为 icon.png ，放入 frontend.h5/assets/ 目录。
- 启动页 ：准备一张 2732x2732 像素的 PNG 图片，重命名为 splash.png ，放入 frontend.h5/assets/ 目录。
npx capacitor-assets generate --android